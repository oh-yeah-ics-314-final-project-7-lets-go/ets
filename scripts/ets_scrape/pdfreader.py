from pathlib import Path

def parse_pdf(path):
    
    return
    
  
  
if __name__ == "__main__":
    downloads_dir = Path('downloads')
    pdf_files = list(downloads_dir.glob('*.pdf'))
    for pds in range(pdf_files):
        parse_pdf(pds)